'use strict';

describe('myApp.Pharmacies module', function() {

  beforeEach(module('myApp.Pharmacies'));

  describe('Pharmacies controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var PharmaciesCtrl = $controller('PharmaciesCtrl');
      expect(PharmaciesCtrl).toBeDefined();
    }));

  });
});
